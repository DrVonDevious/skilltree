{
  "name": "skilltrees",
  "version": "0.1.0",
  "description": "The skilltrees codebase",
  "main": "index.js",
  "scripts": {
    "docker:dev": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build",
    "docker:prod": "docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build",
    "docker:down": "docker-compose down --remove-orphans",
    "prepare": "husky install"
  },
  "author": "william rutland (william.prometheus@gmail.com)",
  "license": "ISC",
  "devDependencies": {
    "husky": "^7.0.4"
  }
}
